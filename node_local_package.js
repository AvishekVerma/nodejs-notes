const os = require('os');

// console.log(os);

// to get the archittecture
console.log(os.arch());

// to get the cpu counts
console.log(os.cpus());

// to get the hostname
console.log(os.hostname());